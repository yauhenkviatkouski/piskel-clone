// import brezenham from './common-utils'; return array of points on path of cursor
import lightenButton from './lighten/lighten';


describe('lightenButton', () => {
  it('Should be Function', () => {
    expect(lightenButton).toBeInstanceOf(Function);
  });
});
